# DATASOURCE (DataSourceConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:mysql://localhost:3306/eshop
spring.datasource.username=root
spring.datasource.password=000000
logging.level.org.springframework=DEBUG

# Hibernate
spring.jba.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Hibernate ddl(create, create-drop, update, validate)
spring.jpa.hibernate.ddl-auto=update

#spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

server.error.include-message=always
#access token from userServiceImpl
application.security.access-token-secret=super_secret_token_and_it_is_very_lon
